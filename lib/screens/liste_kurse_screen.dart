import 'package:flutter/material.dart';

class ListeKurseScreen extends StatelessWidget {
  const ListeKurseScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
